DIRS = camera_pipe

naive:
	    @-for d in $(DIRS); do \
			        make -C $$d naive; \
					        done

polymage:
	    @-for d in $(DIRS); do \
			        make -C $$d polymage; \
					        done
